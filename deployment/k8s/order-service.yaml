apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: inventory-ns
spec:
  type: NodePort
  ports:
    - port: 8002
      targetPort: 8002
      nodePort: 30002
  selector:
    app: order-service
